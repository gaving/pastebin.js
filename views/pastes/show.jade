#status
  - if (d.gravatar)
    span.avatar
      img(src=d.gravatar, title='gravatar')
  span.back
    a(href='/' ) pastes
  - if (d.title)
    span.title
      a(href='/pastes/' + d.id)=d.title
  div.clear

section#content
  #pastes
    script(type='syntaxhighlighter', class='brush: ' + d.lang)=d.body
